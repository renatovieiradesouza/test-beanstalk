files:
    "/etc/cron.d/certbot":
        mode: "000644"
        owner: root
        group: root
        content: |
            56 1 * * * root /opt/certbot/certbot-auto renew --no-self-upgrade
container_commands:            
  03_remove_old_cron:
    command: "rm -f /etc/cron.d/*.bak"    