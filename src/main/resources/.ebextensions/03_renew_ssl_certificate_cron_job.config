files:
  "/etc/cron.d/certbot":
    mode: "000644"
    owner: root
    group: root
    content: |
      # Renew Let's Encrypt certificates twice daily
      0 12 * * * root /usr/bin/certbot renew --quiet --no-self-upgrade
      0 0 * * * root /usr/bin/certbot renew --quiet --no-self-upgrade
